import 'package:flutter/material.dart';

class UIRegisterButton extends StatelessWidget {
  const UIRegisterButton({super.key});

  @override
  Widget build(BuildContext context) {
    return FilledButton.tonal(
      onPressed: () {},
      child: const Text('Register'),
    );
  }
}
