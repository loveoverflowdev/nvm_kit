import 'package:flutter/material.dart';

class UiGridView extends StatelessWidget {
  const UiGridView({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}

class UiGridTile extends StatelessWidget {
  const UiGridTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
