import 'package:flutter/material.dart';

class ActiveInputField extends StatelessWidget {
  const ActiveInputField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
