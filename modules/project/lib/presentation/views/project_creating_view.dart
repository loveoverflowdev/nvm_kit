import 'package:flutter/material.dart';

class ProjectCreatingView extends StatelessWidget {
  const ProjectCreatingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('ProjectCreatingView'),
    );
  }
}
